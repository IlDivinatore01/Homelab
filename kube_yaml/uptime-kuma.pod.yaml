apiVersion: v1
kind: Pod
metadata:
  name: uptime-kuma-pod
spec:
  containers:
  - name: uptime-kuma
    # Pinned to the current beta version we have data for.
    # Floating ':beta' auto-pulls new (potentially breaking) betas. Bump manually.
    image: docker.io/louislam/uptime-kuma:2.1.0-beta.3
    # NET_RAW kept so ICMP ping monitors keep working; everything else dropped.
    securityContext:
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
        - ALL
        add:
        - NET_RAW
    ports:
    - containerPort: 3001
      hostPort: 3001
    volumeMounts:
    - name: tmp
      mountPath: /tmp
    - name: uptime-kuma-data
      mountPath: /app/data
    resources:
      limits:
        memory: 512Mi
        cpu: 200m
      requests:
        memory: 128Mi
        cpu: 50m
  volumes:
  - name: tmp
    emptyDir: {}
  - name: uptime-kuma-data
    hostPath:
      path: /home/osvaldo/podman/data/uptime-kuma
      type: DirectoryOrCreate
  restartPolicy: Always
